{
  "private": true,
  "description": "Haxeshim is distributed through the lix package, not as a standalone npm package. This package.json exists only to provide npm scripts for local development.",
  "scripts": {
    "build": "haxe all.hxml",
    "test": "haxe tests.hxml && node tests-out/RunTests.js",
    "test:e2e": "haxe haxeshim.hxml && node tests/e2e/run.mjs"
  }
}
